lazy-lock.json
deps

# réglages Claude locaux, y compris ceux des plugins sous plugins/
.claude/settings.local.json
/plugins

# clés d'API locales (minuet/DeepSeek, codecompanion)
.env

# Copies de sûreté écrites par « list-dir reseed » ; la base de fusion est semence/.
**/.list/backup/
