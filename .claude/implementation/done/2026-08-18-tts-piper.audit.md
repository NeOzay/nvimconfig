---
slug: tts-piper
---

## 2026-08-18 — clôture — `482b727`

**Verdict** : RÉSERVES

Audit joué sur `git diff master...tts-piper` (3 commits, 23 fichiers, +2183/-1). Le contenu du SHA
audité a été extrait par `git archive` dans un répertoire de travail hors dépôt : l'arbre courant
porte des modifications non commitées (dont deux du chantier, cf. **R1**) qui faussent toute
exécution faite en place.

Contexte d'exécution notable : la machine où tourne cet audit **est** le poste Bazzite du brief
(`/etc/os-release` → `Bazzite 44.20260811.0`), avec `pw-play` présent, le service `tts-piperd`
`active` et le port 7788 lié à `127.0.0.1`. La moitié locale de la chaîne a donc pu être sondée
pour de vrai, ce que le plan n'anticipait pas.

### Vérifications exécutées

- `make test` **au SHA audité** (arbre extrait par `git archive`) → 58 cas, 4 groupes,
  `Fails (0) and Notes (0)`
- `make test` **dans l'arbre de travail courant** → **1 échec** :
  `tests/tts/test_protocol.lua | build_request | déduit la voix de la langue`,
  `Left: "fr_FR-glados-medium" / Right: "fr_FR-siwis-medium"` (cf. **R1**)
- Étape 1 — `test -f plugins/tts.nvim/lua/tts-nvim/init.lua && ! test -d plugins/tts.nvim/.git` → OK
- Étape 2 — `! grep -rq '^M = {}' plugins/tts.nvim/lua/` + `loadfile` de `backends.lua` → OK
- Étape 2 (verif du **plan**, plus stricte que celle du suivi) —
  `grep -rni 'openai\|edge' plugins/tts.nvim/` → une seule occurrence, `LICENSE:27` (cf. **R9**)
- Étape 3 — `! grep -rq 'Job:new' …/init.lua` + `type(require('tts-nvim.client').request)=='function'` → OK
- Étape 4 — `python3 -m py_compile daemon/tts-piperd.py` + présence de `.service` et `README.md` → OK
- Étape 5 — `require('tts-nvim').setup({})` puis `vim.fn.exists(':TTSConfig')==2` → OK
- Étape 6 — `make test` → cf. ci-dessus
- Étape 7 — `nvim --headless -c "lua assert(require('lazy.core.config').plugins['tts.nvim'])" -c qa`
  → exit 0 (config complète chargée)
- Bout en bout du plan — `nvim --headless -c "lua assert(require('tts-nvim'))" -c qa` → exit 0
- Ports des tests — `test_daemon_io.lua:9` → 7799, `test_protocol.lua:128` → 7801, production 7788 :
  distincts, conformément au critère
- Absence de dépendance audio ou Python côté Neovim —
  `grep -rn 'plenary\|ffplay\|ffmpeg\|python\|subprocess' plugins/tts.nvim/lua/ lua/plugins/tts.lua`
  → aucune occurrence
- Absence de branchement contextuel — `grep -rn 'SSH_CONNECTION' plugins/tts.nvim/ lua/plugins/tts.lua`
  → aucune occurrence (seule mention : `docs/plugins/tts.md:21`, en négatif)
- Absence de persistance — `grep -rn 'writefile\|io.open\|stdpath' plugins/tts.nvim/lua/` → aucune
- Refus des adresses non locales — `tts-piperd.py --host 0.0.0.0` →
  `error: le démon ne s'écoute que sur la boucle locale`, exit non nul
- Robustesse du protocole, démon `--dry-run` sur 7812 (requêtes brutes en socket Python) :
  - `pas du json` → `{"ok": false, "error": "requête illisible : Expecting value…"}`
  - `42` → `{"ok": false, "error": "la requête doit être un objet JSON"}`
  - `{"op":"speak","text":"x"}` → `{"ok": false, "error": "aucune voix demandée"}`
  - `{"op":"speak",…,"speed":"vite"}` → `{"ok": false, "error": "could not convert string to float…"}`
  - `{"op":"save",…}` → `{"ok": true, "path": "…/tts.wav"}`
- Démon **de production** (service actif, port 7788) — `{"op":"voices"}` →
  `{"ok": true, "voices": ["fr_FR-glados-medium", "fr_FR-siwis-medium"]}`
- Coût de chargement d'un modèle, hors démon et sans audio (venv de production) →
  `load 0.84 s`, `sample_rate 22050`, `synth 0.17 s` pour une phrase (cf. **R2**)
- Critère « lecture validée à l'oreille, en local et via `ssh -R` » → **NON EXÉCUTÉE** : jugement
  perceptif et session SSH inverse hors de portée de cet audit (cf. **R7**)

### Conformité à l'intention

- Critère « des tests automatisés couvrent la logique du fork et le protocole d'échange, joués en
  loopback sur un port distinct de celui de production » : **atteint, vérifié**. 58 cas dont 27 sur
  le TTS répartis en trois fichiers ; `test_daemon_io.lua` fait un aller-retour réel contre
  `tts-piperd.py --dry-run` sur 7799, `test_protocol.lua` couvre l'encodage, la construction de
  requête, la validation et la cible injoignable sur 7801. La production reste sur 7788.
- Critère « la lecture audio n'est pas testée automatiquement — validation à l'oreille » :
  **respecté dans le code** (aucun test ne joue de son), **invérifiable pour ma part** côté
  validation utilisateur (**R7**).
- Critère « `make test` passe » : **atteint au SHA audité** (0 échec), **en échec dans l'arbre
  courant** du fait de modifications non commitées du chantier lui-même (**R1**).
- Hors-périmètre : **respecté**. Aucun Piper ni dépendance audio côté Neovim ; aucun `edge` /
  `openai` dans le code (seul vestige : `LICENSE`, **R9**) ; aucune écriture disque des réglages ;
  le clone est sans `.git`, sans entrée `.gitmodules` — conforme à l'**amendement du 2026-08-18**
  du suivi, qui prime sur le brief (fork submodule) et est bien reflété dans `CLAUDE.md`.
- Signaux de dérive : **aucun matérialisé**. Pas de branchement `$SSH_CONNECTION` dans
  `lua/plugins/tts.lua` (24 lignes, une seule `dir =`, `cmd` et `opts`) ; aucune dépendance audio
  côté serveur ; un seul transport, en dur dans `client.lua`, sans couche d'abstraction ; le démon
  n'expose que `speak`/`save`/`stop`/`voices` et refuse toute adresse hors boucle locale.
- Symptôme d'origine (« TTS impossible en SSH, `ffplay` côté serveur ») : **traité**. `ffplay` a
  disparu, Neovim n'émet que du JSON sur TCP loopback, la lecture est déportée. La disparition
  effective du symptôme repose sur la validation à l'oreille de la session 2, que je n'ai pas pu
  rejouer (**R7**), mais toute la moitié vérifiable est établie.

### Qualité du code

- **R1** — `plugins/tts.nvim/lua/tts-nvim/config.lua:36` et `lua/plugins/tts.lua:20` (arbre de
  travail, **non commités**) : la voix française par défaut passe à `fr_FR-glados-medium` et
  `speed = 0.85` est ajouté à la spec. Conséquence immédiate : `make test` **échoue** dans l'arbre
  courant, parce que `tests/tts/test_protocol.lua:71` fige la valeur attendue en dur
  (`"fr_FR-siwis-medium"`) au lieu de la lire depuis `config.languages_to_voice`. Deux constats en
  un : (a) un réglage voulu par l'utilisateur est hors du SHA audité et sera perdu ou à recommiter
  à l'aplatissement ; (b) le test est couplé à une valeur de configuration, donc toute retouche
  légitime de la voix par défaut casse la suite. Le critère « `make test` passe » n'est vrai qu'au
  SHA audité.
- **R2** — `plugins/tts.nvim/daemon/tts-piperd.py:149` (`Synthesizer.synthesize` → `self._load`) :
  le chargement du modèle a lieu **dans le fil de la requête**, avant que `dispatch` ne rende
  `{"ok": true}` ; seule la synthèse est paresseuse. Mesuré ici à **0,84 s** cache chaud, en local.
  Le client abandonne à `timeout_ms = 2000` (`config.lua:24`). Sur un premier `:TTS` avec un modèle
  froid (page cache vide, 60 Mo d'`.onnx`), à travers un tunnel SSH, la marge est mince : Neovim
  notifiera « aucune réponse de 127.0.0.1:7788 après 2000 ms » alors que la lecture partira quand
  même. Cas limite non couvert par les tests (le mode `--dry-run` court-circuite `_load`).
- **R3** — `plugins/tts.nvim/lua/tts-nvim/config.lua:63-69` et
  `plugins/tts.nvim/lua/tts-nvim/init.lua:72-86` : dès que `:TTSVoice` a posé `config.voice`, ce
  choix l'emporte définitivement et `:TTSSetLanguage` devient sans effet pour la session — aucun
  moyen de revenir au mode « voix déduite de la langue » sans redémarrer Neovim. Pire,
  `tts_set_language` notifie alors `langue fr (fr_FR-siwis-medium)`, c'est-à-dire une voix qui ne
  sera pas utilisée : le message affirme le contraire du comportement.
- **R4** — `plugins/tts.nvim/lua/tts-nvim/text_processor.lua` : seul module du plugin resté au
  style de l'upstream — indentation à 4 espaces là où les six autres et tout le dépôt sont en
  tabulations, commentaires en anglais, `M.x = function(...)` au lieu de `function M.x(...)`,
  annotations Emmylua absentes sur `process_markdown_simple`, `process_latex_simple` et
  `process_text`. Le fichier a pourtant été touché (la fonction `pandoc` locale, elle, est au style
  du dépôt) : la reprise s'est arrêtée à mi-chemin. Deux défauts hérités qui restent dedans, et
  qui n'ont plus d'upstream pour les corriger : le motif de lien markdown
  `text:gsub("%[(.-)%]%(.-)", "%1")` (ligne 44) n'a pas de parenthèse fermante `%)` et ne consomme
  donc pas la cible du lien ; `pandoc()` (ligne 17-20) appelle `vim.system(…):wait()` **de façon
  bloquante** dans la boucle d'événements, là où l'upstream passait par un `plenary.Job` — sur une
  grosse sélection markdown, Neovim se figera le temps de pandoc.
- **R5** — `plugins/tts.nvim/daemon/tts-piperd.py:234` : `save` écrit toujours
  `<output-dir>/tts.wav`. Deux `:TTSFile` de suite écrasent le premier sans avertissement, et la
  notification côté Neovim (`init.lua:61`) annonce un chemin qui vient d'être détruit. Vérifié en
  `--dry-run` : chemin constant.
- **R8** — `plugins/tts.nvim/lua/tts-nvim/client.lua:69-103` : si `uv.new_timer()` renvoie `nil`,
  le garde-fou de délai n'est simplement pas armé (`if timer then`) et la requête peut rester
  pendante indéfiniment, sans notification ni appel de `on_response`. Chemin d'échec silencieux,
  très improbable, mais traité différemment de `uv.new_tcp()` juste au-dessus, qui lui notifie
  (ligne 64-67) : l'incohérence est ce qui le rend notable.

### Dette induite

- **R6** — `plugins/tts.nvim/LICENSE:27` mentionne encore la bibliothèque `edge_tts` et sa LGPLv3
  alors que le backend `edge` a été supprimé. Cosmétique, et un fichier légal ne se retouche pas à
  la légère, mais c'est désormais une affirmation fausse dans un fichier suivi par ce dépôt.
- **R9** — La vérification de l'étape 2 telle qu'écrite dans le suivi (`tts-piper.md:52`) a laissé
  tomber la clause `! grep -rqi 'openai\|edge'` que portait le plan
  (`.claude/plans/linear-scribbling-papert.md`, étape 2). Je l'ai rejouée : elle échouerait
  aujourd'hui, sur `LICENSE` seul (**R6**). L'affaiblissement est donc bénin dans les faits, mais
  il n'est justifié ni dans le journal de décisions ni dans l'étape : un lecteur ultérieur ne peut
  pas distinguer un assouplissement raisonné d'un oubli. La réserve du `plan-reviewer` sur
  l'étape 4 (démon + unit + doc pour une seule vérification de présence) reste, elle, ouverte et
  assumée dans le suivi.
- Hors ces deux points : rien à signaler. Aucune duplication introduite, aucune abstraction
  gratuite (la table `backends` est un point d'extension explicitement demandé par le brief), le
  couplage nouveau est le seul voulu — Neovim → socket loopback. La doc est fournie et honnête sur
  ce qui n'a pas pu être exécuté à l'écriture, et `CLAUDE.md` comme `docs/plugins/tts.md` sont à
  jour au gabarit du dépôt.

### Réserve d'invérifiabilité

- **R7** — Le critère « lecture validée à l'oreille, en local **et** depuis le VPS via
  `ssh -R 7788:127.0.0.1:7788` » repose entièrement sur la déclaration de session 2 du suivi. Je
  ne peux ni juger un rendu sonore, ni monter la session SSH inverse depuis ici. Ce que j'ai pu
  établir sur cette machine, et qui la corrobore sans la remplacer : Bazzite confirmé, `pw-play`
  présent, `piper` installé, deux modèles `.onnx` dans `~/.local/share/piper-voices`, service
  `tts-piperd` `active`, socket `127.0.0.1:7788` en écoute par un processus `python`, et
  `{"op":"voices"}` répondant correctement contre le démon de production. Le maillon non vérifié
  se réduit donc à « le PCM sort effectivement des haut-parleurs » et « le `RemoteForward` relaie ».

### Bloquants

Aucun. Au SHA audité, tous les critères de réussite vérifiables sont atteints, le hors-périmètre
est intact et aucun signal de dérive ne s'est matérialisé. Le verdict est `RÉSERVES` et non
`FAVORABLE` pour deux raisons cumulées : **R1** — l'arbre courant est rouge et contient des
réglages du chantier non commités, à trancher avant tout aplatissement sur `master` — et **R7** —
le critère central du brief (le son sort, en local comme à travers SSH) n'est pas vérifiable par
cet audit.

## 2026-08-18 — clôture (2ᵉ passage) — `ee8f95d`

**Verdict** : RÉSERVES

Audit complet de `git diff master...tts-piper` (4 commits, 24 fichiers, +2403/-1), pas une simple
re-vérification de **R1** et **R2**. Le delta depuis `482b727` porte sur six fichiers, dont deux de
code (`daemon/tts-piperd.py`, `lua/tts-nvim/config.lua`), un de test
(`tests/tts/test_protocol.lua`), la spec (`lua/plugins/tts.lua`), le suivi et le rapport d'audit
précédent — désormais versionné sur la branche.

Cette fois l'arbre de travail **ne porte plus aucune modification du chantier** :
`git diff ee8f95d -- plugins/tts.nvim lua/plugins/tts.lua tests/ docs/ init.lua CLAUDE.md` est
vide, les seules modifications restantes (`codediff`, `trouble`, `cokeline`, `lualine`, un plan)
sont étrangères au chantier. Les commandes ont donc pu être jouées en place, sans extraction.

### Vérifications exécutées

- `make test` (arbre courant, identique au SHA audité) → 58 cas, 4 groupes,
  `Fails (0) and Notes (0)` — **R1 levée**
- `git diff ee8f95d -- plugins/tts.nvim lua/plugins/tts.lua tests/ docs/plugins/tts.md init.lua CLAUDE.md`
  → vide : les réglages `fr_FR-glados-medium` et `speed = 0.85` sont bien **entrés dans le commit**
- Latences mesurées contre un **démon réel** (venv de production, modèles réels, port 7813, lancé
  avec un `PATH` vide pour que `pw-play` soit introuvable et qu'aucun son ne sorte) :
  - `{"op":"voices"}` → **1,6 ms**
  - `speak`, modèle **froid** (premier chargement de `fr_FR-glados-medium`) → **0,4 ms**, `{"ok": true}`
  - `speak`, modèle **chaud** → **0,7 ms**, `{"ok": true}`
  - `speak` sur voix absente → **0,7 ms**,
    `{"ok": false, "error": "modèle introuvable : /home/Benoit/.local/share/piper-voices/nexiste_pas.onnx"}`
  - `{"op":"stop"}` → **0,9 ms**
  - Journal du démon dans le même essai : `DEBUG Guessing voice config path: …glados-medium.onnx.json`
    puis `INFO speak : fr_FR-glados-medium, 22050 Hz, 1 canal/aux, volume 1.00` — le chargement a
    bien lieu **après** la réponse, dans le fil de lecture. **R2 levée**, et au-delà de ce qui était
    demandé : le coût n'est pas réduit, il a quitté le chemin de la requête (0,84 s → < 1 ms).
- Étape 1 — `test -f …/init.lua && ! test -d …/.git` → OK
- Étape 2 — `! grep -rq '^M = {}' …/lua/` + `loadfile` de `backends.lua` → OK
- Étape 2 (clause du **plan**) — `grep -rni 'openai\|edge' plugins/tts.nvim/` → toujours une seule
  occurrence, `LICENSE:27` (**R6**/**R9** inchangés)
- Étape 3 — `! grep -rq 'Job:new' …/init.lua` + `client.request` est une fonction → OK
- Étape 4 — `python3 -m py_compile daemon/tts-piperd.py` + présence du `.service` et du `README.md`
  → OK (le `__pycache__/` produit par la compilation a été supprimé ; `git status plugins/tts.nvim`
  → vide, rien n'a été laissé dans l'arbre)
- Étape 5 — `setup({})` puis `vim.fn.exists(':TTSConfig')==2` → OK
- Étape 6 — `make test` → cf. ci-dessus
- Étape 7 — `nvim --headless -c "lua assert(require('lazy.core.config').plugins['tts.nvim'])" -c qa`
  → OK
- Critère « lecture validée à l'oreille, en local et via `ssh -R` » → **NON EXÉCUTÉE**, pour la même
  raison qu'au premier passage (**R7** maintenue)

### Conformité à l'intention

- Critère « tests automatisés sur la logique et le protocole, en loopback sur un port distinct » :
  **atteint, vérifié**. Inchangé sur le fond : 58 cas, ports 7799 et 7801 contre 7788 en production.
- Critère « la lecture audio n'est pas testée automatiquement » : **respecté**.
- Critère « `make test` passe » : **atteint et vérifié dans l'arbre courant**, ce qui n'était pas le
  cas au passage précédent. C'est le seul changement de statut d'un critère.
- Hors-périmètre : **toujours respecté**. Le correctif n'ajoute aucune dépendance côté Neovim, rien
  n'est écrit sur disque, `daemon/tts-piperd.py` reste le seul fichier Python et il n'a gagné qu'un
  `stat`.
- Signaux de dérive : **aucun matérialisé**. Le démon n'a pas gagné d'opération, pas de transport,
  pas de branchement contextuel.
- Symptôme d'origine : **traité**, avec la même réserve d'invérifiabilité (**R7**).

### Suite donnée aux constats précédents

- **R1** — **levée**, vérifiée. `tests/tts/test_protocol.lua:68-74` lit désormais
  `config.languages_to_voice[config.backend]["fr"]` et le commente ; les deux réglages non commités
  sont dans `ee8f95d`. `make test` passe sur l'arbre.
- **R2** — **levée**, vérifiée et mesurée. Le découpage retenu est le bon : `ensure_available()` (un
  `stat`) garde l'erreur « voix absente » visible du client — confirmé, 0,7 ms —, tandis que
  `_load` et la synthèse partent dans le fil de lecture. Le `player.stop()` remonté dans `dispatch`
  répond bien à l'objection qu'il aurait sinon fallu attendre le chargement pour couper la phrase
  en cours (cf. **R11** pour sa contrepartie).
- **R3** — **tient**. `config.lua:63-69` inchangé : `config.voice` posé par `:TTSVoice` masque
  toujours `:TTSSetLanguage` pour la session, sans retour possible au mode déduit.
- **R4** — **tient**. `text_processor.lua` toujours à 4 espaces (37 lignes indentées ainsi),
  `:wait()` bloquant en ligne 20, motif de lien markdown toujours sans `%)`.
- **R5** — **tient**. `tts-piperd.py:248` : `save` écrit toujours `<output-dir>/tts.wav`.
- **R6** — **tient**. `LICENSE:27` mentionne encore `edge_tts`.
- **R7** — **tient**, inchangée : la validation à l'oreille et le trajet `ssh -R` restent hors de
  portée d'un audit.
- **R8** — **tient**. `client.lua:99` : `if timer then` laisse toujours la requête sans délai de
  garde si `uv.new_timer()` échoue.
- **R9** — **tient**. L'étape 2 du suivi (`tts-piper.md:52`) n'a pas récupéré la clause
  `! grep -rqi 'openai\|edge'` du plan, et l'écart n'est toujours pas justifié par écrit.

### Qualité du code — constats nouveaux

- **R10** — `plugins/tts.nvim/daemon/tts-piperd.py:141-143` : `ensure_available()` ne contrôle que
  `<voix>.onnx`. Piper a aussi besoin de `<voix>.onnx.json` — le journal du démon le dit lui-même
  (`Guessing voice config path: …onnx.json`), et les deux modèles installés ici ont bien les deux
  fichiers. Une voix téléchargée à moitié (`.onnx` présent, `.json` manquant) passe donc le
  contrôle : le client reçoit `{"ok": true}`, l'échec part au journal, et le symptôme redevient
  « aucun son » — précisément ce que la journalisation de la session 2 cherchait à éliminer. Un
  second `exists()` dans la même méthode fermerait le trou au même coût.
- **R11** — `plugins/tts.nvim/daemon/tts-piperd.py:257` : le `player.stop()` remonté dans `dispatch`
  coupe la lecture en cours **dès qu'une demande est acceptée**, avant de savoir si la nouvelle
  synthèse aboutira. Constaté dans mon essai avec `pw-play` introuvable : réponse `{"ok": true}`,
  lecture précédente coupée, échec visible au seul journal. C'est le compromis assumé par le
  commentaire du code (ne pas retarder l'interruption), mais il élargit la fenêtre où le client
  croit à un succès qui n'aura pas lieu — à lire avec **R10**, qui en est le cas d'entrée le plus
  probable.
- **R12** — Le chemin d'erreur ajouté par le correctif n'a **aucune couverture automatisée** :
  `ensure_available()` retourne immédiatement en `--dry-run` (`tts-piperd.py:139-140`), donc
  `tests/tts/test_daemon_io.lua`, qui ne lance le démon que dans ce mode, ne peut pas l'exercer. La
  réponse « modèle introuvable » n'est établie que par ma vérification manuelle contre un démon
  réel. Le critère « des tests couvrent le protocole d'échange » reste atteint, mais sa branche la
  plus récente vit hors suite.
- **R13** — `tests/tts/test_protocol.lua:71-74` (correctif de **R1**) lit la voix attendue dans la
  table que le code testé consulte lui-même. Le test ne discrimine encore que parce que
  `piper_model` (`fr_FR-siwis-medium`, `config.lua:46`) diffère de `languages_to_voice.piper.fr`
  (`fr_FR-glados-medium`, `config.lua:36`) : si les deux venaient à coïncider, il passerait aussi
  bien avec un `current_voice()` retombé sur le repli, c'est-à-dire dans le cas même qu'il est censé
  distinguer du test voisin. Le découplage demandé est obtenu ; sa robustesse tient à une
  coïncidence près.

### Dette induite

Rien de nouveau. Le correctif n'introduit ni duplication, ni abstraction, ni couplage : il déplace
du travail entre deux fils et ajoute un `stat`. La dette relevée au premier passage (**R4**, **R6**,
**R9**) est inchangée.

### Bloquants

Aucun. Tous les critères vérifiables sont atteints et vérifiés sur l'arbre courant, le
hors-périmètre est intact, aucun signal de dérive ne s'est matérialisé, et les deux réserves qui
motivaient le premier verdict ont été traitées — **R2** au-delà de ce qui était demandé. Le verdict
reste `RÉSERVES` et non `FAVORABLE` pour une seule raison, la même qu'au premier passage : **R7**,
le critère central du brief (le son sort, en local comme à travers le tunnel) n'est vérifiable que
par l'utilisateur, qui l'a déclaré atteint en session 2. Les constats **R10** à **R13** sont à
arbitrer, pas à corriger avant clôture ; **R3** à **R6**, **R8** et **R9** restent ouverts tels
qu'ils étaient.
